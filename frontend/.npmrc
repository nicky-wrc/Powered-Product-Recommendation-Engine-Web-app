# fdir (via tinyglobby) optional-peers picomatch ^3||^4 while micromatch pins ^2.x; npm hoisting
# makes `npm ci` / `npm ls` fail without this (see eslint-config-next / typescript-eslint tree).
legacy-peer-deps=true
