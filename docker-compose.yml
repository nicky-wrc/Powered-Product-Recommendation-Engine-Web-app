services:
  db:
    image: pgvector/pgvector:pg16
    container_name: recengine-db
    environment:
      POSTGRES_USER: recengine
      POSTGRES_PASSWORD: recengine
      POSTGRES_DB: recengine
    ports:
      - "5433:5432"
    volumes:
      - recengine_pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U recengine -d recengine"]
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: recengine-redis
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5

volumes:
  recengine_pgdata:
